@using MDD4All.SpecIF.ViewModels.Metadata
@using MDD4All.SpecIF.DataModels.Manipulation
@page "/statementClasses"
@layout MetadataLayout
@if (DataContext.EditModeActive)
{
    @*<PropertyClassEditor PropertyClassViewModel="@DataContext" />*@
}
else
{
    <h3><span class="bi bi-arrow-left-right" aria-hidden="true"></span>&nbsp;@L["Title.StatementClassBrowser"]</h3>
    @*<div>
        <button type="button" class="btn btn-success">
            @L["Button.NewResourceClass"]
        </button>
    </div>
    <br />
    <h4>@L["Title.PropertyClasses"]</h4>*@

    <table class="table">
        <thead>
            <tr>
                <th>@L["Label.Icon"]</th>
                <th>@L["Label.Title"]</th>
                <th>@L["Label.ID"]</th>
                <th>@L["Label.Revision"]</th>
                <th>@L["Label.Description"]</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (StatementClassViewModel viewModel in DataContext.StatementClasses)
            {
                <tr>
                    <td><ResourceClassIcon Icon="@viewModel.ResourceClass.Icon" /></td>
                    <td><strong>@viewModel.ResourceClass.Title</strong></td>
                    <td>@viewModel.ResourceClass.ID</td>
                    <td>@viewModel.ResourceClass.Revision</td>
                    <td>@((MarkupString)viewModel.ResourceClass.Description.GetDefaultStringValue())</td>
                </tr>
            }
        </tbody>
    </table>
}